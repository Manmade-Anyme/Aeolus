# Changelog

All notable changes to AEOLUS. Format loosely follows [Keep a Changelog](https://keepachangelog.com/).

## [Unreleased]

### Added
- 2026-07-03 — Project scaffold: folder structure, pipeline docs (`docs/`), PM directives TASK-001..013, ADR/report templates, project `CLAUDE.md`. No code yet.
- 2026-07-03 — TASK-001 Supabase schema: 5 migration files (`supabase/migrations/`) for `signal_snapshots`, `state_transitions`, `daily_outlook`, `outcome_labels` + enum types; typed pydantic models (`src/aeolus/storage/models.py`); ADR at `directives/adr/TASK-001_supabase-schema.md`. `system_status`/`market_state` enforced as structurally distinct Postgres enum types. Model-layer tests pass (16/16); DB-level DDL tests written, pending `TEST_DATABASE_URL`.
- 2026-07-03 — TASK-001 live DB verification: schema applied to the real (shared, Ares) Supabase project via SQL Editor; access pattern switched to `supabase-py` + anon key (RLS disabled), matching Ares convention — direct-Postgres/CLI approach wasn't available in this environment. New live test suite `tests/storage/test_supabase_integration.py` (8/8 pass) replaces the unusable psycopg-based one.
- 2026-07-03 — TASK-002 Dhan API v2 ingestion layer: `src/aeolus/ingestion/` — credentials loaded from the shared Supabase `api_keys` table (same external refresh daemon as Ares, no new secret scheme); NIFTY spot + current-month futures resolved dynamically against Dhan's live scrip master CSV (roll-aware); WebSocket live feed (spot LTP, futures LTP, 5-level depth via `Full` packets) with exponential-backoff reconnect; REST-polled option chain (OI/greeks/IV per strike) with 2-consecutive-short-read partial-response detection; elapsed-time heartbeat staleness tracker feeding `system_status` (OK/STALE/DISCONNECTED), fully decoupled from `market_state`. `futures_basis` (futures − spot) included per OPEN_DECISIONS #4. ADR at `directives/adr/TASK-002_dhan-ingestion.md`. Full test suite (36/36) includes live integration tests against real Supabase, the real Dhan scrip master, and — during live market hours — the real Dhan WebSocket + REST APIs end-to-end.
- 2026-07-03 — TASK-002 amendment: added `india_vix` to `IngestionSnapshot` (Dhan `IDX_I` segment, security_id 21 — same subscription path as spot) and `IngestionService.lot_size` (resolved from the scrip master's `SEM_LOT_UNITS`, session-scoped). Unblocks TASK-003 (volatility) and TASK-004 (gamma) ADRs, both of which had flagged these as missing from ingestion rather than working around it. No new segment, vendor, or dependency — both fields come from the same compact scrip master CSV `instruments.py` already parses. 37/37 tests passing.
- 2026-07-03 — TASK-003 volatility signals: `src/aeolus/signals/volatility.py` — four pure sub-signal functions (IV percentile/rank, IV-vs-RV spread, VIX level+ROC, expected-move-consumed ratio), all on the `SignalResult` contract (`src/aeolus/signals/contract.py`, shared by TASK-004..007). No I/O — all trailing history caller-supplied. `iv_rv_spread` redesigned before implementation per human field feedback: scored primarily on IV's own cycle-to-cycle trend (falling IV = NO-GO regardless of absolute level), not the static IV-vs-RV level. Interim reason-string templater at `src/aeolus/explain/reason.py`, signature-compatible with TASK-010. ADR at `directives/adr/TASK-003_volatility-signals.md`. 24 new tests (61/61 total), ruff/mypy clean. See `reports/debug/TASK-003_debug-report.md`, `reports/qa/TASK-003_qa-report.md`.
- 2026-07-03 — TASK-004 gamma signals: `src/aeolus/signals/gamma.py` — `gex_regime` (signed, dollar-scaled net GEX across the chain; dealer-long-gamma-on-calls/short-on-puts sign convention; magnitude normalized via shared `_percentile_rank`) and `spot_distance_from_flip` (interpolated zero-gamma flip strike, strike-domain proxy; magnitude-only, sign-independent distance scoring — near the flip is the pin/magnet NO-GO zone). `_clamp01` promoted from `volatility.py` into `signals/contract.py`, now shared. ADR at `directives/adr/TASK-004_gamma-signals.md`. 12 new tests (73/73 total), ruff/mypy clean. See `reports/debug/TASK-004_debug-report.md`, `reports/qa/TASK-004_qa-report.md`.
- 2026-07-03 — TASK-005 OI structure signals: `src/aeolus/signals/oi_structure.py` — `pcr_level_and_roc` (rate-of-change drives sub_score, direction-agnostic), `oi_buildup_classification` (long buildup/short covering/short buildup/long unwinding, OI-weighted, price-direction signal is `futures_ltp` per human decision resolving the ADR's blocking dependency — no ingestion amendment needed), `oi_wall_proximity_and_strength` (mirrors TASK-004's flip-distance design language), `max_pain_drift` (standard payout-minimization method, hand-verified). First module needing cycle-relative `current`/`previous` `IngestionSnapshot` pairs, caller-supplied. ADR at `directives/adr/TASK-005_oi-structure-signals.md`. 23 new tests (96/96 total), ruff/mypy clean. See `reports/debug/TASK-005_debug-report.md`, `reports/qa/TASK-005_qa-report.md`.
- 2026-07-03 — TASK-002 amendment: added `volume`, `total_buy_quantity`, `total_sell_quantity`, `day_high`, `day_low` to `IngestionSnapshot` — all five already arriving in the futures `Full` packet, previously unparsed by `feed_ws.py`. Human-approved ingestion amendment (larger than the earlier VIX/lot_size one) unblocking TASK-006. New unit-level parsing tests (`test_feed_ws_parsing.py`) verify against the `dhanhq` SDK's own `process_full()` field names, no live market hours needed.
- 2026-07-03 — TASK-006 order flow signals: `src/aeolus/signals/order_flow.py` — `cvd_direction_and_divergence` (cycle-granularity CVD approximation via tick-rule volume attribution, confirms/diverges from price — "price progress without CVD confirmation is a fragile move"), `delta_imbalance_and_absorption` (confirms-extreme vs opposes-extreme/absorption polarity, human-confirmed 2026-07-03), `volume_participation_range` (session-relative range from first X% of cumulative volume, resolving OPEN_DECISIONS #1; degenerate-range mitigation via `min_range_width`). CVD reset-across-reconnect handled by construction, not special-cased. ADR at `directives/adr/TASK-006_order-flow-signals.md`. 25 new signal tests + 3 ingestion parsing tests (124/124 total), ruff/mypy clean. See `reports/debug/TASK-006_debug-report.md`, `reports/qa/TASK-006_qa-report.md`.
- 2026-07-03 — TASK-002 amendment: added `expiry_date` to `IngestionSnapshot`, sourced from `IngestionService`'s existing (but previously unsurfaced) Dhan `expiry_list` resolution — already holiday-shift aware, no new API call, no in-repo calendar needed. Unblocks TASK-007's `dte()`.
- 2026-07-03 — TASK-007 context signals (last of the five category modules): `src/aeolus/signals/context.py` — `dte` (plain date subtraction against Dhan's own resolved expiry, exempted from the standard `SignalResult` contract per OPEN_DECISIONS #2), `build_volume_price_histogram` + `value_area` (cycle-sampled volume-at-price approximation, POC + 70%-of-volume band), `prior_day_profile_shape` (trend vs balanced/rotational via range-expansion + close-location, `DayProfileShape` enum returned standalone alongside its `SignalResult`, polarity human-confirmed), `gap_classification` (no_gap/gap-and-go/gap-and-fill vs yesterday's value area; `session_open` deliberately caller-captured rather than the still-unverified Full-packet `open` field), `futures_basis_drift` (resolves OPEN_DECISIONS #4, reuses the confirm/diverge shape from `cvd_direction_and_divergence`). ADR at `directives/adr/TASK-007_context-signals.md`. 22 new tests + `expiry_date` threaded through existing `IngestionSnapshot` constructions (146/146 total), ruff/mypy clean. See `reports/debug/TASK-007_debug-report.md`, `reports/qa/TASK-007_qa-report.md`.
- 2026-07-03 — TASK-008 composite scorer & state machine (first module with real I/O): `src/aeolus/engine/` — `Engine.start/run_cycle/end_session`, `EngineState` (seeds every cross-session trailing history + prior-day context from Supabase, rebuilds session-scoped state on a same-day restart), `scorer.py` (equal-weighted category aggregation, `safe_call` isolates a crashing sub-signal to a neutral reading, N-cycle hysteresis). Config matches ARES's exact `pydantic-settings` pattern (`config/tuning.py` + `config/profiles.py` — `EXPIRY_CONFIG`/`NON_EXPIRY_CONFIG`, hardcoded dual instances, not YAML), human-directed. `expected_move_consumed_ratio` sourced via a VIX-based expected-move approximation (no straddle premium in ingestion). End-of-session cleanup is in-memory only (`Engine.end_session()`, scheduler-triggered at market close) — `signal_snapshots` is never pruned, preserving Build Prompt 1's recomputability guarantee. ADR at `directives/adr/TASK-008_composite-state-machine.md`. 25 new tests, including live Supabase integration tests exercising real inserts/reads/hysteresis flips (171/171 total), ruff/mypy clean. See `reports/debug/TASK-008_debug-report.md`, `reports/qa/TASK-008_qa-report.md`.
- 2026-07-03 — TASK-003 amendment: added `implied_expected_move` (VIX-based expected-move approximation, factored out of TASK-008's `Engine.run_cycle`) to `src/aeolus/signals/volatility.py` — additive only, no existing signature changed. Shared by TASK-008 (live) and TASK-009 (pre-market) instead of two copies of the same formula.
- 2026-07-03 — TASK-009 pre-market outlook generator: `src/aeolus/outlook/` — `OutlookGenerator.run()` (single pre-open invocation, own dedicated Supabase seeding, deliberately not coupled to TASK-008's `EngineState`), `archetype.py` (`score_archetypes`/`primary_and_secondary` — uniform-prior-plus-multiplicative-nudges model across the 7 `DayArchetype` values, yesterday's trend-exhaustion as the headline driver per Spec §5.1, every nudge factor an explicitly-flagged unbacktested placeholder). Two real gaps found and resolved: the directive's claimed "TASK-003 straddle-history function" never existed (resolved via `implied_expected_move`); `daily_outlook`'s schema has no column for a full archetype distribution or secondary call (packed into `contributing_inputs`, no migration). Idempotent via upsert-on-`session_date`, preserving any TASK-012 `realized_archetype` backfill. ADR at `directives/adr/TASK-009_premarket-outlook.md`. 17 new tests, including live Supabase integration tests (188/188 total), ruff/mypy clean. See `reports/debug/TASK-009_debug-report.md`, `reports/qa/TASK-009_qa-report.md`.
- 2026-07-03 — TASK-010 explainability: `src/aeolus/explain/reason.py` — TASK-003's interim `template_reason` stub promoted to the real, canonical templater (signature/output byte-identical, no signal-module changes needed); new `explain_transition(from_state, to_state, trigger_categories, composite_score, confirmation_cycles)` fills the composite-level gap in Spec §10 — cites the category/categories driving a confirmed state flip (selection stays TASK-008's territory, this only templates the string), sorts categories alphabetically for determinism, falls back to an explicit "broad-based shift across categories" string when no single category individually crossed TASK-008's 0.1 margin. `engine.py`'s `StateTransition.reason` now calls this instead of a generic `"composite=X confirmed after N cycles"` string with no category names. Added `hypothesis` dev dependency for the directive's required determinism property tests. ADR at `directives/adr/TASK-010_explainability.md`. 5 new tests including 2 Hypothesis property tests (193/193 total), ruff/mypy clean. See `reports/debug/TASK-010_debug-report.md`, `reports/qa/TASK-010_qa-report.md`.
- 2026-07-03 — TASK-011 Discord output formatter & dispatch: `src/aeolus/output/discord.py` — `DiscordDispatcher` with `post_outlook`/`post_transition`/`post_system_status`, two webhook URLs (market vs system-status — separate channels, not just format-only distinction) so "market is dead" (NO_GO) can never be visually confused with "feed is dead" (STALE/DISCONNECTED). Discord embeds (not plain content) for per-category structure and the 2000-char limit; per-category breakdown reuses `SignalSnapshot.raw_readings`/`reasons` as-is via a `SUB_SIGNAL_NAMES` allowlist (no new category→sub-signal mapping needed). New `ARCHETYPE_STATE_LEAN` mapping (human-approved judgment call, no spec precedent) answers the confirm/diverge note against the morning Outlook — `mixed`-lean archetypes (`breakout_transition`, `event_gap`) render "not directly comparable" rather than a forced binary call. Bounded retry (3 attempts, exponential backoff) only for safe-non-delivery cases (timeout/429-honoring-`Retry-After`/5xx); non-retryable responses (e.g. 400) raise immediately; exhausted retries raise `DiscordDeliveryError` rather than dropping silently. `httpx` promoted from an indirect to a direct dependency. ADR at `directives/adr/TASK-011_discord-dispatch.md`. 11 new tests against `httpx.MockTransport` (204/204 total), ruff/mypy clean. See `reports/debug/TASK-011_debug-report.md`, `reports/qa/TASK-011_qa-report.md`.
- 2026-07-03 — TASK-012 outcome-label backfill job: `src/aeolus/jobs/` — `OutcomeBackfillJob.run(session_date)` writes `outcome_labels` for both `signal_snapshots` and `state_transitions` (forward-matched within a ±5 min tolerance to +15/+30/+60 min targets; no match → that horizon is skipped, never fabricated) and backfills `daily_outlook.realized_archetype`. `realized_move` is real (`futures_ltp` delta); `straddle_price_change` reuses the already-established `implied_expected_move` VIX-based proxy a third time (no new formula, no new data collection) — caught and fixed a real bug during testing where `run()` had conflated `current_iv` (ATM option IV) with `india_vix` (VIX index), which would have silently produced wrong labels. New `classify_realized_archetype` (`src/aeolus/jobs/realized_archetype.py`) maps a completed session to one of the 7 `DayArchetype` values via Spec §4's own Directional×Volatility cross-table plus two heuristic overrides (`breakout_transition`, `event_gap`); `double_distribution` is an explicit documented known limitation (needs a persisted full-session volume histogram that doesn't exist) — falls through to the base quadrant rather than faking a detection. Schema amendment: `supabase/migrations/0007_outcome_labels_idempotency.sql`'s partial unique indexes turned out not to satisfy PostgREST's `ON CONFLICT` inference (confirmed live, `42P10`) — fixed via `0008_fix_outcome_labels_idempotency_constraint.sql`, plain unique constraints instead (standard SQL's `NULL <> NULL` already gives the needed guarantee without a partial predicate). ADR at `directives/adr/TASK-012_outcome-backfill.md`. 17 new tests including 3 live Supabase integration tests (221/221 total), ruff/mypy clean. See `reports/debug/TASK-012_debug-report.md`, `reports/qa/TASK-012_qa-report.md`.
- 2026-07-04 — TASK-013 orchestration/scheduler (final module): `src/aeolus/scheduler/` — `Scheduler.run()`, the single entrypoint that wires `IngestionService`, `Engine`, `OutlookGenerator`, `DiscordDispatcher`, and `OutcomeBackfillJob` into one bounded trading session, then returns. Deployment model (human-directed): Fly.io, scaled up/down by an external cron mirroring the sister ARES project — the process itself is bounded to one session and exits cleanly so the Fly machine scales back to 0, rather than an always-on idle-polling server. `NseCalendar` (`src/aeolus/scheduler/calendar.py`) is the only clock-reading module in the codebase (constraint #2's sole exception) — trading-day/session-hours data comes from `config/nse_holidays.json`, populated with the real, live-fetched 2026 NSE FO-segment holiday calendar (`scripts/fetch_nse_holidays.py`, NSE's own public holiday-master endpoint — not fabricated dates). Small additive amendment to TASK-008: `Engine.run_cycle` now returns `tuple[SignalSnapshot, StateTransition | None]` instead of just `SignalSnapshot`, so the scheduler knows when to post to Discord without a redundant query. Every `Scheduler` collaborator (including the clock) is constructor-injectable, specifically so orchestration logic — timing decisions, Discord dispatch, per-cycle/per-post error containment — is testable without live Dhan/Supabase/wall-clock dependencies; no fully live end-to-end test of a real session exists, an explicitly accepted gap (see QA report) pending actual deployment. `scripts/run_aeolus.py` is the process entrypoint Fly's cron-scaled machine will invoke. Fly.io/cron setup itself deferred by human direction — `docs/OPEN_DECISIONS.md` #5. ADR at `directives/adr/TASK-013_orchestration.md`. 11 new scheduler tests + 25 regression-checked engine tests (232/232 total), ruff/mypy clean. See `reports/debug/TASK-013_debug-report.md`, `reports/qa/TASK-013_qa-report.md`.
- 2026-07-04 — TASK-011 amendment: Discord readability pass, live-tested against the real channels over several human-reviewed rounds. Human-readable labels replace raw snake_case identifiers everywhere in `discord.py` (categories, sub-signals, `DayArchetype` values) — display-only, `template_reason`'s deterministic string (constraint #3) untouched. Sub-signal breakdown lines now show just the score (`sub_score` read directly off `raw_readings`), not `raw_value`/`reference_band` clutter. `gex_regime` lines get a regime-type annotation ("Short Gamma / Trending" vs "Long Gamma / Pinning") derived from the sign already documented in its own docstring. Fixed a duplication bug where the transition embed's description repeated `composite=X` (once from the description prefix, once from `explain_transition`'s own string). Color-coding went through two iterations: ANSI SGR codes in ```ansi fenced blocks first (renders on desktop/web only — confirmed via live mobile screenshot that Discord mobile shows raw escape codes instead), reverted to 🟢/🟡/🔴 emoji indicators (same favorable/neutral/unfavorable threshold `engine.py` uses for `trigger_categories`), confirmed rendering correctly on mobile via a second screenshot. ADR amendment recorded in `directives/adr/TASK-011_discord-dispatch.md`. `tests/output/test_discord.py` updated for the new format (12 tests, 233/233 total), ruff/mypy clean.

- 2026-07-04 — TASK-014 ML schema + retention job (first ML overlay module): migrations `0009..0011` (`ml_feature_store`, `ml_model_registry`, `ml_anomaly_scores`) — `jsonb`/`timestamptz`/enum-reuse conventions from TASK-001, no FKs to engine tables so trimming `signal_snapshots` can never cascade into or block on `ml_*`. Typed pydantic models `MLFeatureRow`/`MLModelVersion`/`MLAnomalyScore` at `src/aeolus/ml/models.py` (new package, deliberately separate from `storage/models.py` — no engine file imports `aeolus.ml`), plus `ML_PROTECTED_TABLES` documenting the module's own permanent-corpus exception. `RetentionJob` at `src/aeolus/jobs/retention.py` (engine-side, not `aeolus.ml`, since it deletes `signal_snapshots` rows): batched age-trim (90d default) on `signal_snapshots`/`ml_anomaly_scores`, count-prune (30 versions default, never below 1) on `ml_model_registry`, structural `PROTECTED_TABLES` denylist assertion before every delete. ADR at `directives/adr/TASK-014_ml-schema-cleanup-denylist.md`. 7 contract tests pass; 3 live-Supabase retention tests written but pending migrations 0009-0011 being hand-applied via the Supabase SQL Editor (anon key has no DDL rights — same one-time gate every prior migration went through). ruff/mypy clean. See `reports/debug/TASK-014_debug-report.md`, `reports/qa/TASK-014_qa-report.md`.

- 2026-07-04 — TASK-015 ML feature extractor: `src/aeolus/ml/features.py` — `extract_features(SignalSnapshot) -> dict[str, float | None] | None` (pure, no I/O, no fitting), 15 features (5 category sub-scores + composite + 9 raw-reading leaves incl. `vix_level_and_roc`/`pcr_level_and_roc`'s composite level+RoC pairs flattened from `raw_value`/`context`), `FEATURE_ORDER`/`FEATURE_SET_VERSION` derived from one `_ACCESSORS` mapping so key order can't drift from the extraction dict; `None` for `STALE`/`DISCONNECTED` snapshots (a broken feed isn't a market anomaly); `standardize(raw, Scaler) -> list[float]` applies a stored μ/σ only, never fits (fitting is TASK-017's). `config_type` deliberately absent from `FEATURE_ORDER` — it selects the model, isn't a feature. ADR at `directives/adr/TASK-015_ml-feature-extractor.md`. 12 new tests incl. 2 Hypothesis determinism properties (19/19 ml tests), ruff/mypy clean. See `reports/debug/TASK-015_debug-report.md`, `reports/qa/TASK-015_qa-report.md`.

- 2026-07-04 — TASK-016 ML feature-store sync: `src/aeolus/ml/store.py` — `FeatureStore.append(snapshot, scaler)` (live per-cycle write; refuses and skips when TASK-015 extraction returns `None` or any feature is `None`), `sync_eod(session_date)` (EOD catch-up: anti-joins `signal_snapshots` against already-stored `source_snapshot_id`s, appends the gap, returns count), `load_window(config_type, window_days)` (trainer's read path: most recent N distinct session dates, config-filtered). Idempotency by construction: both paths funnel through one upsert keyed on migration 0009's `UNIQUE(source_snapshot_id)` with `ignore_duplicates=True` — same mechanism TASK-012 settled on. Strictly read-only against `signal_snapshots`. ADR at `directives/adr/TASK-016_ml-feature-store-sync.md`. 4 new live-Supabase tests written per Definition of Done, pending migrations 0009-0011 hand-apply (same blocker as TASK-014); 19/19 non-DB ml tests unaffected. ruff/mypy clean. See `reports/debug/TASK-016_debug-report.md`, `reports/qa/TASK-016_qa-report.md`.

- 2026-07-04 — TASK-017 ML model trainer: `src/aeolus/ml/trainer.py` — `ModelTrainer.train_all()`/`train(config_type)`, end-of-day batch retrain, no online learning. Per config: `FeatureStore.load_window` → drop-incomplete → warm-up gate (`sample_count >= 10×n_features` AND `trading_day_count >= 15`, both `MLTuning`-configured) → fit μ/σ scaler (σ floored via TASK-015's `SIGMA_FLOOR`) → fit `sklearn.ensemble.IsolationForest` (`n_estimators=200`, fixed `random_state`) → score training set, negate (higher = more anomalous, the binding convention for TASK-018/019) → empirical `flag_threshold`/`clear_threshold` (top 5%/10%, configurable) → joblib→base64 serialize → versioned `ml_model_registry` insert (`version = prev + 1`, no pruning in v1). Two configs trained independently; one's failure never blocks the other. New `src/aeolus/ml/config.py` (`MLTuning`, pydantic-settings style: `window_days=30`, `flag_pct=0.05`, `clear_pct=0.10`, warm-up factors, `n_estimators`, `random_state`). Added `scikit-learn>=1.5`, `numpy`, `joblib` to `pyproject.toml`. ADR at `directives/adr/TASK-017_ml-model-trainer.md`. 5 new live-Supabase tests written (blocked on the same pending migration 0009-0011 hand-apply as TASK-014/016) plus a fake-client sanity run that caught and fixed a real bug (`joblib.dumps`/`loads` don't exist — fixed via `io.BytesIO` + `joblib.dump`/`load`) and confirmed the fit/threshold/round-trip path end-to-end. ruff/mypy clean. See `reports/debug/TASK-017_debug-report.md`, `reports/qa/TASK-017_qa-report.md`.

- 2026-07-04 — TASK-018 ML live scorer: `src/aeolus/ml/scorer.py` — `LiveScorer.score_cycle(snapshot) -> ScoreEvent | None`, per-cycle live scoring path: refuse `STALE`/`DISCONNECTED` or any missing feature (no row, no event) → standardize with the latest cached model's scaler → score via `IsolationForest.score_samples` (negated, higher = more anomalous) → persist an `ml_anomaly_scores` row every scored cycle → step `AnomalyState`'s debounce+hysteresis state machine (`ANOMALY_ENTER` only on `score >= flag_threshold` from NORMAL, `ANOMALY_CLEAR` only on `score < clear_threshold` from ANOMALOUS, optional `min_dwell_cycles` blocking a flip until the current state has held N cycles). Model cache keyed per `config_type` with a TTL (`MLTuning.model_cache_ttl_seconds=300`, elapsed-time only, not a clock-of-day read) rather than a registry read every cycle; `sklearn_version`/`feature_set_version` mismatch at load → treated as no-model (WARMING_UP), loud log, per TASK-014/015's compat guard. `top_features` (top-3 by `|z|`) populated only on transition rows, per this task's ADR. Never writes engine tables; the scorer never talks to Discord — returns events for TASK-021's hook to drive TASK-019/020. ADR at `directives/adr/TASK-018_ml-live-scorer.md`. 11 new no-DB unit tests (state machine + top-features ranking) + 6 new live-Supabase tests, all 17 passing after migrations landed (see Fixed section below). A fake-client sanity run caught and fixed a synthetic-test-fixture bug (`gex_magnitude`'s `abs()` in `extract_features` wasn't respected by the training-matrix generator) before it could reach the committed suite. ruff/mypy clean. See `reports/debug/TASK-018_debug-report.md`, `reports/qa/TASK-018_qa-report.md`.
- 2026-07-04 — TASK-019 ML explainability: `src/aeolus/ml/explain.py` — `top_contributors(z_by_feature, k=3)` (descending `|z|`, ties broken by `FEATURE_ORDER` position for total-order determinism), `anomaly_reason(contributors, score, flag_threshold, model_version)` and `clear_reason(score, clear_threshold, model_version)` (fixed-precision templated strings, e.g. `anomalous — driven by vix_level (+3.2σ), gex_magnitude (-2.8σ) | score 0.812 vs flag 0.650 | model v3`). Pure functions over TASK-018's `ScoreEvent.z_by_feature`; v1 uses plain per-feature `|z|` rather than Mahalanobis attribution (ADR-recorded v2 upgrade path, ties to Open Decision #10's ensemble toggle). Deliberately separate from `src/aeolus/explain/reason.py` (engine-owned; same templating discipline, separate module so ML vocabulary doesn't leak into the engine). ADR at `directives/adr/TASK-019_ml-explainability.md`. 8 new no-DB tests: ranking, k-cap, exact-tie determinism, golden-string pinning (enter + clear), empty-contributors `ValueError` guard, Hypothesis identical-input determinism property, and a monkeypatch-based proof that explanation cannot influence a flag decision already made by `AnomalyState.step`. ruff/mypy clean. See `reports/debug/TASK-019_debug-report.md`, `reports/qa/TASK-019_qa-report.md`.
- 2026-07-04 — TASK-020 ML Discord output: `src/aeolus/ml/output.py` — `MLDiscordDispatcher` with `post_anomaly`/`post_clear`/`post_warmup_progress`/`post_golive`, mirroring `aeolus.output.discord`'s httpx-webhook/retry/embed shape but kept as a separate class so engine output code stays ML-free. Every message titled with a `🔬 ML` prefix and a color outside the engine's exact palette; the anomaly advisory alone carries the mandatory `"advisory only — does not change engine state"` footer. Two small, documented deviations from the ADR's literal API (see debug report): `post_clear` gained a `config_type` parameter (the ADR's own "message content" list requires it, but neither `ScoreEvent` nor `clear_reason` carries it); the warm-up once-per-day dedup guard keys on the caller-supplied `day` counter rather than a wall-clock date, so this module never reads the system clock at all. Never decides whether to post — only how; that remains TASK-018/021's job. `MLTuning` gained `ml_discord_webhook_url` (fallback-to-market-webhook wiring left to TASK-021's constructor call, per the ADR). ADR at `directives/adr/TASK-020_ml-discord-output.md`. 10 new tests via `httpx.MockTransport` (same convention as TASK-011's suite): golden content per message type, distinct-color/prefix/footer assertions, warm-up dedup (same-day suppress, next-day pass, cross-config independence), unset-webhook no-op, retry/`MLDiscordDeliveryError` behavior. ruff clean; mypy clean on all production code (a pre-existing, repo-wide `**dict` fixture-pattern mypy quirk reproduces identically in the already-merged `tests/output/test_discord.py`, not a regression). See `reports/debug/TASK-020_debug-report.md`, `reports/qa/TASK-020_qa-report.md`.
- 2026-07-04 — TASK-021 ML orchestration hooks (last of the ML overlay tasks): `src/aeolus/ml/hooks.py` — `MLHooks` facade composing TASK-016..020's collaborators into two total (never-raising) methods: `start_session(session_date)` (resets `LiveScorer` state; detects+announces go-live/warm-up via a registry lookup — `version == 1` observed at session start unambiguously means "just left warm-up," no persisted marker needed) and `on_cycle(snapshot)` (`FeatureStore.append` → `LiveScorer.score_cycle` → on a transition, resolve the registry's `version`/thresholds by `model_version_id` and post via TASK-019's templates + TASK-020's dispatcher); every internal step independently try/except-wrapped. `src/aeolus/scheduler/scheduler.py` — minimal wiring diff: new `ml_hooks: MLHooksProtocol | None = None` (a local structural `Protocol`, not an `aeolus.ml` import, so the scheduler never depends on the ML module at runtime) called at 3 sites (`start_session` before the live loop, `on_cycle` after each `engine.run_cycle`, `on_end_of_day` after `_run_backfill`); new `retention_job: RetentionJob | None = None` wired to run unconditionally, strictly after `ml_hooks.on_end_of_day` returns, completing the `sync → retrain → cleanup` order for the first time (RetentionJob, built in TASK-014, had not been wired into the scheduler until now). Every ML call site double-wrapped (inside `MLHooks` and again at the scheduler call site) so a broken or non-conforming hook can never crash the engine loop or session teardown. ADR at `directives/adr/TASK-021_ml-orchestration-hooks.md`. New tests: 18 no-DB (`tests/ml/test_hooks.py`, fake collaborators — failure isolation per step, EOD ordering, go-live/warm-up detection); 1 live-Supabase (`tests/ml/test_hooks_integration.py`, real fitted model + HTTP-mocked dispatcher, full `on_cycle` chain end to end); 4 new scheduler tests (`ml_hooks=None` parity, 3-call-site wiring, hooks-raise-everywhere containment, retention-after-ml-hooks ordering) alongside all 11 pre-existing TASK-013 scheduler tests passing unmodified; 2 new AST-based import-boundary tests (`tests/test_ml_import_boundary.py`) confirming zero `aeolus.ml` imports across `engine/signals/ingestion/outlook/explain/output/jobs/` and specifically `scheduler.py`. ruff/mypy clean. Full repo suite: 323 passed, 1 pre-existing unrelated failure. See `reports/debug/TASK-021_debug-report.md`, `reports/qa/TASK-021_qa-report.md`.

**This completes the ML anomaly overlay (TASK-014 through TASK-021).**

### Fixed
- 2026-07-04 — Migrations `0009-0011` (`ml_feature_store`/`ml_model_registry`/`ml_anomaly_scores`, TASK-014) hand-applied to the live Supabase project, unblocking all 17 previously-pending live tests across TASK-014/016/017/018. Re-running surfaced 2 real test-only bugs: (1) sequential-delete cleanup blocks in `test_store_integration.py`/`test_trainer_integration.py`/`test_scorer_integration.py` let one table's delete failure mask another's, leaking 10 orphaned synthetic `signal_snapshots` rows across many earlier blocked runs — fixed by independently guarding every cleanup delete, orphaned rows removed (user-authorized); (2) `test_scorer_integration.py`'s normal/spike/hover/clear sequence test reused snapshot objects across cycles, giving tied `ts` values and unstable query ordering — fixed by giving each cycle its own snapshot with a distinct `ts`. No production code changed. Full suite: 280 passed (1 pre-existing, unrelated live-Dhan-API failure). See `reports/debug/TASK-018_debug-report.md`.
- 2026-07-03 — TASK-001: `outcome_labels_has_source` CHECK constraint conflicted with the `ON DELETE SET NULL` FK on the same table, blocking deletes of referenced `signal_snapshots` rows entirely. Found via live testing. Fixed in `supabase/migrations/0006_fix_outcome_labels_source_check.sql`; the "at least one source" invariant now lives solely in the `OutcomeLabel` pydantic validator.
- 2026-07-03 — TASK-002: recurrence of the TASK-001 `.pth`-hidden-flag editable-install issue; additionally hardened pytest to no longer depend on the editable-install `.pth` at all (`pythonpath = ["src"]` in `pyproject.toml`).

### Resolved
- 2026-07-03 — OPEN_DECISIONS #2 (DTE-graduated weighting): binary for v1, ARES `config_profiles.py` pattern — two complete expiry/non-expiry tables, no partial overrides; table selected from TASK-007's DTE output (holiday-aware). Graduated weighting deferred to v2 pending TASK-012 outcome-label data. Unblocks TASK-008. See `docs/OPEN_DECISIONS.md`.
- 2026-07-03 — OPEN_DECISIONS #3 (historical backfill): no, live-forward only. See `docs/OPEN_DECISIONS.md`.
- 2026-07-03 — OPEN_DECISIONS #4 (futures basis): include now as a raw field on every TASK-002 ingestion snapshot; drift/trend interpretation deferred to the signal layer. See `docs/OPEN_DECISIONS.md`.
