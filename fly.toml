app = 'aeolus'
primary_region = 'sin'

[build]
  dockerfile = './Dockerfile'

[[services]]
  internal_port = 8080
  processes = ['app']

  [[services.ports]]
    port = 8080
    protocol = 'tcp'

[vm]
  memory = '1gb'
  cpus = 1
